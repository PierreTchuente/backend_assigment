version: '3.9'

services:

  backend_api:
    build: ./backend_api
    container_name: backend_api
    restart: always
    environment:
      - APP_NAME=BACKEND_API
    expose:
      - 8080


  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "80:80"
